name: Semver Label Check
on:
  pull_request:
    types: [opened, labeled, unlabeled, synchronize]

jobs:
  check-version:
    uses: lowranceworks/common-workflows/.github/workflows/validations/version-label-check.yaml@main
    permissions:
      issues: write
      pull-requests: write
      contents: read
