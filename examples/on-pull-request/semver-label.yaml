name: Semver Label
on:
  pull_request:
    types: [labeled, unlabeled]
jobs:
  check-semver-label:
    uses: lowranceworks/common-workflows/.github/workflows/semver-label.yaml@main
    permissions:
      issues: write
      pull-requests: write
      contents: read
