name: Build and Publish Zip Archive
on:
  pull_request:
    types: [closed]
jobs:
  build:
    needs: release
    if: success() && github.event.pull_request.merged == true
    uses: ticketing-platform/common-workflows/.github/workflows/build-zip-upload-artifactory.yaml@main
    secrets:
      ARTIFACTORY_USERNAME: ${{ secrets.ARTIFACTORY_USERNAME }}
      ARTIFACTORY_PASSWORD: ${{ secrets.ARTIFACTORY_PASSWORD }}
